# Protocol resources

This directory will contain versioned, validated protocol packs. Protocol
paths, XML vocabulary, mappings, enumerations, units, templates, and safe
response data belong here rather than in Python source.

No pack is committed until it is derived from sanitized observations and can
cover a complete thermostat poll cycle. See
[`../docs/implementation-plan.md`](../docs/implementation-plan.md).
